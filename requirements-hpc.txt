# requirements-hpc.txt
# --------------------
# Python 3.10, linux-x86_64, CUDA 12.1 wheels for the IITD Padum cluster.
#
# Diverges from requirements.txt only in the torch stack: Padum's driver
# stack targets CUDA 12, not the CUDA 13 pins used in the Mac dev flow.
#
# Consumed by scripts/build_hpc_bundle.sh via:
#   pip download --python-version 3.10 --platform manylinux2014_x86_64 \
#                --only-binary=:all: -d wheels/ -r requirements-hpc.txt
#   pip download --python-version 3.10 --platform manylinux2014_x86_64 \
#                --only-binary=:all: -d wheels/ \
#                --extra-index-url https://download.pytorch.org/whl/cu121 \
#                torch==2.4.1+cu121 torchvision==0.19.1+cu121

# ---- core -----------------------------------------------------------------
annotated-types==0.7.0
certifi==2026.6.17
charset-normalizer==3.4.7
click==8.4.1
filelock==3.29.0
fsspec==2026.4.0
gitdb==4.0.12
GitPython==3.1.50
h5py==3.16.0
idna==3.18
Jinja2==3.1.6
MarkupSafe==3.0.3
mpmath==1.3.0
networkx==3.4.2
numpy==2.2.6
packaging==26.2
pillow==12.2.0
platformdirs==4.10.0
protobuf==7.35.1
pydantic==2.13.4
pydantic_core==2.46.4
PyYAML==6.0.3
requests==2.34.2
scipy==1.15.3
sentry-sdk==2.63.0
smmap==5.0.3
sympy==1.14.0
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.7.0

# ---- torch stack (CUDA 12.1, matches Padum's driver stack) ---------------
# NOTE: these two are pulled from the pytorch cu121 index, not PyPI. The
# build script passes the extra index URL explicitly.
torch==2.4.1+cu121
torchvision==0.19.1+cu121
triton==3.0.0

# ---- utilities ------------------------------------------------------------
tqdm
wandb
fastapi
uvicorn[standard]
